[project]
name = "ontorag-catalog"
version = "0.1.0"
description = "OntoRAG baseline ontology catalog, served as REST and MCP"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.129.0",
    "fastmcp>=2.14.5",
    "rdflib>=7.6.0",
]
